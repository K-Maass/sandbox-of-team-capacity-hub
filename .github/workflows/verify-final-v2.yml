name: Verify Team Capacity Hub
on:
  push:
    branches:
      - main
      - verify/final-v2
  pull_request:
    branches:
      - main
permissions:
  contents: read
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - name: Install dependencies
        run: bun install
      - name: Test
        run: bun test
      - name: Typecheck
        run: bunx tsc --noEmit
      - name: Production build
        run: bun run build
